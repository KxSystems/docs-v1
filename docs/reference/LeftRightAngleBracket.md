&lt;&gt; is very closely related to [=](Reference/EqualsSymbol "wikilink"), and simply returns the opposite result:

    q)a:1 2 3
    q)(a<>2) ~ not a=2
    1b

For binary values, &lt;&gt; is the same as exclusive or (xor).
